import styled from "styled-components";
const Wrapper = styled.div`

button {
    color:var(--black);
    font-size:2rem;
    margin-right:1rem;
}
.dark{
    color:var(--black);
}
.light{
    color:var(--white);
}
`

export default Wrapper